# analytics-dashboard

Admin Analytics tab + REST. Metric cards (total reactions/captures, sentiment rate, capture rate), top posts table, daily series chart. Reads from daily_agg + captures only. 30-day default Free window, manage_options gated.
