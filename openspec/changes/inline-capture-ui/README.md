# inline-capture-ui

Inline capture form rendered after a positive reaction. Email + consent checkbox + submit. Reuses POST /capture endpoint from Session 4. Full-sentence errors via role=alert; focus management on open and on submit success.
