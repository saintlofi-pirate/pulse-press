# csv-export

Admin CSV export of moonfarmer_reactions_lead_capture_captures with consent metadata. GET /moonfarmer-reactions-lead-capture/v1/captures.csv, manage_options-only. Streaming response. Filterable column set so Pro can layer ESP-sync status.
