# csv-export

Admin CSV export of pulsepress_captures with consent metadata. GET /pulsepress/v1/captures.csv, manage_options-only. Streaming response. Filterable column set so Pro can layer ESP-sync status.
