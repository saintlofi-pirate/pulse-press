# schema-and-migrations

Create the three PulsePress custom tables (reactions, captures, daily_agg) and a versioned migration runner driven by the pulsepress_db_version option. Reserve retention and delete-on-uninstall settings without exposing UI.
