# schema-and-migrations

Create the three Moonfarmer Reactions Lead Capture custom tables (reactions, captures, daily_agg) and a versioned migration runner driven by the moonfarmer_reactions_lead_capture_db_version option. Reserve retention and delete-on-uninstall settings without exposing UI.
