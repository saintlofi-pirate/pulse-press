# email-capture-storage

POST /pulsepress/v1/capture endpoint, consent fields stored against pulsepress_captures, HMAC-hashed IP+UA for fraud review with 30-day purge cron, full-sentence error messages for a11y.
