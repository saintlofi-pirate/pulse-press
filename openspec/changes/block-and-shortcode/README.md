# block-and-shortcode

Dynamic Gutenberg block pulsepress/reactions and [pulsepress] shortcode. Same widget container output. Auto-insert skips posts that already contain either marker. Display precedence: shortcode > block > post-meta > global.
