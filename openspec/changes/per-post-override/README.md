# per-post-override

Per-post Auto/On/Off override via post meta box, dynamic public-post-type list in admin settings, new hide_on_post_types hard-block setting. VisibilityResolver encapsulates the precedence: post-meta > global hide list > global auto-insert > default. Consulted by block, shortcode, and auto-insert.
