{
  "name": "moonfarmer-reactions-lead-capture",
  "version": "0.1.0",
  "description": "Moonfarmer Reactions Lead Capture — reactions, inline email capture, and analytics for WordPress.",
  "private": true,
  "license": "GPL-2.0-or-later",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "dependencies": {
    "preact": "^10.22.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "@preact/preset-vite": "^2.9.0",
    "typescript": "^5.4.0",
    "vite": "^5.3.0"
  }
}
